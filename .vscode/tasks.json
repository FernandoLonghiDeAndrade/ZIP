{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd build 2>nul || mkdir build && cd build && cmake .. && cmake --build .",
            "problemMatcher": []
        }
    ]
}