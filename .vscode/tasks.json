{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build 2>nul || mkdir ${workspaceFolder}/build && cd ${workspaceFolder}/build && cmake .. && cmake --build .",
            "problemMatcher": []
        }
    ]
}